<h1> <%= @user.username %> </h1>
<h1> <%= @user.email %> </h1>
<p> Total events created <%= @user.events.count %>
<% @user.events.each do |event, msg|%>
  <p class = 'text-danger'><%=link_to event.title, event_path(event) %> /<small>Created
  <%= time_ago_in_words(event.created_at)%> ago</small>
  </p>
 

<% end%>

